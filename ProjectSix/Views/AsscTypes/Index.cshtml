@model IEnumerable<ProjectSix.Models.AsscType>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.AsscTypeName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.AsscTypeName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.AsscTypesID }) |
            @Html.ActionLink("Details", "Details", new { id=item.AsscTypesID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.AsscTypesID })
        </td>
    </tr>
}

</table>
