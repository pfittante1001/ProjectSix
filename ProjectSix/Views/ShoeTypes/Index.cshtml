@model IEnumerable<ProjectSix.Models.ShoeType>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ShoesTypeName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ShoesTypeName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ShoeTypeID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ShoeTypeID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ShoeTypeID })
        </td>
    </tr>
}

</table>
